Ingress Replication is used when multicast is disabled on a VXLAN EVPN network, Switches copy a message to every endpoint instead of sending BUM traffic which scales terribly.

VXLAN uses UDP to encapsulate

Symmetric Integrated Routing and Bridging is if the VNI stays the same, and assymetric introduces a Layer 3 VNI for transport

