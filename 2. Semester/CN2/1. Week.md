Quic works on the transport layer

### OSPF
lower administrative distance = more trustworthy

```Networking
R 10.0.0.0 [120/1] via 192.168.0.10
```
This means that `120` is RIP (administrative distance) and `1` is the metric